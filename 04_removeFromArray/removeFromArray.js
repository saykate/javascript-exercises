const removeFromArray = function(arr, check) {
   const newArr = arr.map(check) {
    for (let i = 0; i < arr.length; i++);
    if (check !== arr[i]) {
        return arr[i]
    };
    return newArr
}
};

// Do not edit below this line
module.exports = removeFromArray;
